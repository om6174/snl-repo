export enum UserRole {
    ADMIN = 1,
    TRAINER = 2,
    USER = 3
}

export enum TrainerStatus {
    LOGGED_IN = 1,
    LOGGED_OUT = 2,
    DISABLED = 3
}

export enum VariationStatus {
    LIVE = 1,
    ARCHIVED = 2,
}

export enum GameplayStatus {
    LIVE = 1,
    STARTED = 2,
    PAUSED = 3,
    FINISHED = 4,
    ARCHIVED = 5,
}